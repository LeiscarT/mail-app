<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MailApp.Views.AddMailPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" >
            <Entry Text="{Binding Title}" Placeholder="Title"></Entry>
            <Entry Text="{Binding Description}" Placeholder="Description"></Entry>
            <Entry Text="{Binding From}" Placeholder="From"></Entry>
            <Button Text="Add" Command="{Binding AddCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage> 